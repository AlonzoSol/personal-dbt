models:
  - name: stg_chase
    columns:
      - name: primary_key
        data_tests:
          - primary_key

  - name: stg_fidelity
    columns:
      - name: primary_key
        data_tests:
          - unique
          - primary_key