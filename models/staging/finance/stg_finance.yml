models:
  - name: stg_chase
    columns:
      - name: primary_key
        data_tests:
          - unique
          - not_null

  - name: stg_fidelity
    columns:
      - name: primary_key
        data_tests:
          - unique
          - not_null